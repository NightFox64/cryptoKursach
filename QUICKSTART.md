# 🚀 Быстрый старт

## Запуск системы за 3 шага

### 1️⃣ Запустите Docker (RabbitMQ + PostgreSQL)

```bash
docker-compose up -d
```

Проверьте статус:
```bash
docker-compose ps
```

### 2️⃣ Запустите сервер

```bash
cd ChatServer
dotnet run
```

Сервер запустится на `http://localhost:5280`

### 3️⃣ Запустите клиент (в новом терминале)

```bash
cd ChatClient
dotnet run
```

---

## 📱 Первый запуск

1. **Зарегистрируйте первого пользователя**:
   - Нажмите "Register"
   - Логин: `alice`
   - Пароль: `password123`

2. **Запустите второго клиента** (новый терминал):
   ```bash
   cd ChatClient
   dotnet run
   ```

3. **Зарегистрируйте второго пользователя**:
   - Логин: `bob`
   - Пароль: `password456`

4. **Добавьте контакт** (от Alice):
   - "Add Contact" → введите `bob`

5. **Подтвердите контакт** (от Bob):
   - В списке контактов будет запрос от Alice
   - Нажмите "Accept"

6. **Создайте чат** (от Alice или Bob):
   - Выберите контакт
   - "Create Chat"
   - Выберите алгоритм (RC6 или LOKI97)

7. **Отправьте сообщение**! 🎉

---

## 🔍 Проверка RabbitMQ

Откройте в браузере: http://localhost:15672
- Логин: `admin`
- Пароль: `admin123`

В разделе "Queues" вы увидите очереди для каждого чата: `chat.queue.{chatId}`

---

## 🛑 Остановка

```bash
# Остановить Docker контейнеры
docker-compose down

# Удалить данные (опционально)
docker-compose down -v
```

---

## ⚠️ Troubleshooting

### RabbitMQ недоступен?
- Клиент автоматически переключится на polling режим
- Сообщения будут доставляться с задержкой ~1 секунда

### Ошибка подключения к PostgreSQL?
- Проверьте, что Docker запущен: `docker-compose ps`
- Проверьте порт: `netstat -ano | findstr :5432` (Windows)

### Сборка не работает?
- Убедитесь, что установлен .NET 10.0 SDK: `dotnet --version`
- Восстановите пакеты: `dotnet restore`

---

## 📖 Полная документация

См. [README.md](README.md) для детальной информации.
