# Курсовой проект по криптографии
## Защищенный чат с использованием RC6 и LOKI97 (Вариант 13)

### 📋 Описание проекта

Комплекс приложений для организации защищенного обмена сообщениями с использованием:
- **Симметричные алгоритмы шифрования**: RC6, LOKI97
- **Протокол обмена ключами**: Диффи-Хеллман
- **Режимы шифрования**: ECB, CBC, PCBC, CFB, OFB, CTR, Random Delta
- **Режимы набивки**: Zeros, ANSI X.923, PKCS7, ISO 10126
- **Брокер сообщений**: RabbitMQ
- **СУБД**: PostgreSQL

---

## 🏗️ Архитектура

### Компоненты системы:

1. **ChatServer** - серверное приложение (ASP.NET Core Web API)
2. **ChatClient** - клиентское приложение (WPF)
3. **RabbitMQ** - брокер сообщений для асинхронной передачи шифротекста
4. **PostgreSQL** - база данных для хранения пользователей, контактов, чатов и истории сообщений

---

## 🚀 Установка и запуск

### Предварительные требования:

- **.NET 10.0 SDK** или выше
- **Docker Desktop** (для RabbitMQ и PostgreSQL)
- **Visual Studio 2022** или **Rider** (рекомендуется)

### Шаг 1: Запуск инфраструктуры (RabbitMQ + PostgreSQL)

Откройте терминал в корневой директории проекта и выполните:

```bash
docker-compose up -d
```

Это запустит:
- **RabbitMQ** на портах:
  - `5672` - AMQP протокол
  - `15672` - Management UI (http://localhost:15672)
  - Логин: `admin`, Пароль: `admin123`
  
- **PostgreSQL** на порту:
  - `5432` - база данных
  - Логин: `postgres`, Пароль: `postgres`
  - База данных: `ChatDb`

### Шаг 2: Запуск сервера

```bash
cd ChatServer
dotnet run
```

Сервер запустится на:
- `http://localhost:5280`
- `https://localhost:7252`

### Шаг 3: Запуск клиента

Откройте новый терминал:

```bash
cd ChatClient
dotnet run
```

Или запустите через Visual Studio/Rider.

---

## 📖 Использование

### Регистрация и авторизация:

1. При первом запуске откроется окно входа
2. Нажмите **"Register"** для создания нового пользователя
3. Введите логин и пароль
4. После регистрации войдите в систему

### Работа с контактами:

1. В главном окне нажмите **"Add Contact"**
2. Введите логин контакта
3. Контакт должен подтвердить запрос (требуется второй клиент)

### Создание чата:

1. Выберите контакт из списка
2. Нажмите **"Create Chat"**
3. Выберите алгоритм шифрования (RC6 или LOKI97)
4. Откроется окно чата

### Настройка алгоритма:

В окне чата:
1. **Settings** → **Algorithm Settings**
2. Выберите:
   - Режим шифрования (ECB, CBC, PCBC, CFB, OFB, CTR, Random Delta)
   - Режим набивки (Zeros, ANSI X.923, PKCS7, ISO 10126)

### Отправка сообщений:

- **Текст**: введите в текстовое поле и нажмите "Send"
- **Файлы**: нажмите "Attach" и выберите файл
- **Изображения**: поддерживаются `.jpg`, `.png`, `.gif` (отображаются как картинки)

---

## 🔒 Безопасность

### Обмен ключами (Диффи-Хеллман):

1. При открытии чата автоматически инициируется протокол DH
2. Клиент генерирует пару ключей (приватный, публичный)
3. Сервер генерирует свою пару и параметры (p, g)
4. Вычисляется общий секрет
5. Из секрета выводится сеансовый ключ через SHA256

### Шифрование сообщений:

1. Сообщение шифруется выбранным алгоритмом (RC6/LOKI97)
2. Применяется выбранный режим шифрования
3. Шифротекст отправляется на сервер через REST API
4. Сервер публикует шифротекст в RabbitMQ
5. Получатель получает сообщение из RabbitMQ в реальном времени
6. Дешифрование происходит локально

### Хранение паролей:

- Пароли хешируются с солью (SHA256)
- В БД хранится только `PasswordHash + Salt`

---

## 🛠️ Технологии

### Серверная часть:
- **ASP.NET Core 10.0** - Web API
- **Entity Framework Core** - ORM
- **PostgreSQL** - СУБД
- **RabbitMQ.Client 7.2.0** - клиент для брокера сообщений
- **JWT** - аутентификация

### Клиентская часть:
- **WPF** - пользовательский интерфейс
- **Entity Framework Core** - локальная БД
- **RabbitMQ.Client 7.2.0** - получение сообщений в реальном времени
- **HttpClient** - REST API клиент

### Криптография:
- **RC6** - собственная реализация (128-bit блок, 128-bit ключ, 20 раундов)
- **LOKI97** - собственная реализация (64-bit блок, 256-bit ключ, 16 раундов)
- **Диффи-Хеллман** - собственная реализация (512-bit)

---

## 📂 Структура проекта

```
cryptoLabs/
├── ChatServer/              # Серверное приложение
│   ├── Controllers/         # REST API контроллеры
│   ├── Services/            # Бизнес-логика
│   ├── Data/                # EF Core DbContext
│   └── Models/              # Модели данных
├── ChatClient/              # Клиентское WPF приложение
│   ├── Services/            # API клиент, шифрование, RabbitMQ
│   ├── Data/                # Локальная БД
│   ├── Converters/          # WPF конвертеры
│   └── *.xaml               # UI окна
├── RC6/                     # Реализация RC6
├── LOKI97/                  # Реализация LOKI97
├── Diffie-Hellman/          # Реализация DH
├── CipherModes/             # Режимы шифрования и набивки
│   └── Modes/               # ECB, CBC, PCBC, CFB, OFB, CTR, Random Delta
└── docker-compose.yml       # Docker конфигурация
```

---

## 🧪 Тестирование

Запуск юнит-тестов:

```bash
dotnet test ChatClient.UnitTests/ChatClient.UnitTests.csproj
dotnet test ChatServer.Tests/ChatServer.Tests.csproj
```

---

## 🔧 Конфигурация

### Сервер (appsettings.json):

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=chatserver_db;Username=postgres;Password=Ichiho64"
  },
  "JwtSettings": {
    "Secret": "YOUR_SECRET_KEY"
  },
  "RabbitMQ": {
    "Host": "localhost",
    "Port": 5672,
    "Username": "admin",
    "Password": "admin123",
    "VirtualHost": "/",
    "ExchangeName": "chat.exchange",
    "QueuePrefix": "chat.queue"
  }
}
```

### Клиент:

RabbitMQ настройки можно изменить в `ChatWindow.xaml.cs`:
```csharp
private bool _useRabbitMQ = true; // false для polling
```

---

## 📊 Особенности реализации

### ✅ Выполненные требования:

1. **Два симметричных алгоритма** - RC6, LOKI97 ✅
2. **Протокол Диффи-Хеллмана** - собственная реализация ✅
3. **REST API сервер** - ASP.NET Core ✅
4. **Брокер сообщений** - RabbitMQ ✅
5. **Асинхронная обработка** - async/await, RabbitMQ consumer ✅
6. **7 режимов шифрования** - ECB, CBC, PCBC, CFB, OFB, CTR, Random Delta ✅
7. **4 режима набивки** - Zeros, ANSI X.923, PKCS7, ISO 10126 ✅
8. **Клиентское приложение** - WPF с полным функционалом ✅
9. **Локальная СУБД** - PostgreSQL + EF Core ✅
10. **IoC/DI** - встроенный в ASP.NET Core ✅
11. **Отображение изображений** - декодирование base64 ✅

### 🎯 Дополнительные возможности:

- **Fallback механизм**: если RabbitMQ недоступен, используется polling
- **История сообщений**: сохраняется локально и на сервере
- **Красивый UI**: логин отправителя отображается рядом с сообщениями
- **ProgressBar**: отображение прогресса операций
- **Обработка ошибок**: graceful degradation

---

## 🐛 Известные ограничения

1. Отмена операций шифрования/дешифрования не реализована полностью
2. При недоступности RabbitMQ автоматически переключается на polling

---

## 👨‍💻 Автор

Вариант 13: LOKI97 + RC6

---

## 📝 Лицензия

Курсовой проект по криптографии (2025-2026 уч. г.)
